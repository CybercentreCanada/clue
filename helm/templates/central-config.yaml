apiVersion: v1
kind: ConfigMap
metadata:
  name: enrichment-server-conf
{{- $Global := . }}
data:
  config.yml: |-
    {{- toYaml .Values.config | nindent 4 }}

  classification.yml: |-
    {{- toYaml .Values.classification | nindent 4 }}
